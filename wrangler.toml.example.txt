# Worker-only template (NOT used by Cloudflare Pages build).
#
# To deploy the Cloudflare Worker later:
# 1) Copy this file to `wrangler.toml`
# 2) Add secrets with `wrangler secret put ...`
# 3) Run `wrangler deploy`
#
# IMPORTANT: Do not commit real SUPABASE_URL/SUPABASE_KEY values.

name = "cloud-federated-rag-worker"
main = "worker.js"
compatibility_date = "2024-11-01"

# Optional plain var:
# [vars]
# SUPABASE_TABLE = "document_chunks"

# Required secrets (run from this directory):
#   wrangler secret put SUPABASE_URL
#   wrangler secret put SUPABASE_KEY
#
# Deploy:
#   wrangler deploy
